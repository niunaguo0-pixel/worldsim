# 2026-08-11 工作日志

## 平台与引擎 pivot → Unity 桌面（用户决定）
- 用户决定 pivot：目标平台 Web(H5) → **桌面 PC**；引擎 Phaser 3 → **Unity**。
- 原因：用户不会操作 Unity Hub GUI；改用 **Unity CLI** 技能（命令行替代 Unity Hub 全部管理功能 + `unity mcp` 操控编辑器），规避 GUI 依赖。
- 已加载 Unity CLI 技能：C:\Users\guowang\.workbuddy\skills\unity-cli\SKILL.md

## Unity 环境诊断结果
- CLI 版本 1.0.0-beta.3，路径 C:/Users/guowang/AppData/Local/Unity/bin/unity.exe
- 编辑器安装路径 C:\Program Files\Unity\Hub\Editor

## Unity 登录 ✅
- 首次 `auth login` 后台超时（5 分钟无回调），用户未在浏览器完成登录。
- 第二次 `auth login` 成功：用户用 **Google 账号** OAuth 登录 Unity ID（Unity 内部关联为 果牛娜 / niunaguo0@gmail.com）。
- `auth status` 确认：已以 果牛娜 niunaguo0@gmail.com 身份登录。

## Unity 编辑器安装 ✅
- 版本 **6000.0.81f1**（Unity 6 LTS），安装到 `C:\Program Files\Unity\Hub\Editor\6000.0.81f1`。
- 下载+安装约 37 分钟，后台任务 LVOrzg 完成。

## Unity 许可证激活 ✅
- 首次建项目失败（exit 198），日志显示 `No valid Unity Editor license found`。
- `unity license activate --personal --accept-eula` 激活 Unity Personal 许可证成功。

## Unity 项目创建 ✅
- 项目名：**WorldSim**，路径 `C:/Users/guowang/Desktop/11/WorldSim`。
- 模板：**Universal 3D (URP)** — `com.unity.template.urp-blank`，2D/2.5D/3D 通吃。
- 包依赖：URP 17.0.4 + AI Navigation 2.0.0 + Input System 1.8.1 + Timeline 1.8.6 + uGUI 2.0.0。
- 首次创建因无许可证失败（残留目录已清理），激活许可证后重试成功。

## Unity MCP 配置 ✅
- 已写入 `~/.workbuddy/mcp.json`，注册 `unity` MCP 服务器。
- 命令：`unity mcp --project-path C:/Users/guowang/Desktop/11/WorldSim`
- 作用：AI 智能体可通过 MCP 直接操控 Unity 编辑器（建场景、导资源、调组件），用户无需碰 GUI。
- **注意**：MCP 服务器需要用户在 WorkBuddy 连接器管理页面手动 Trust 才能激活。

## 项目目录骨架 ✅
- 工作区层：`design/concept`、`design/gdd`、`art`、`production`、`docs/architecture`
- Unity 工程层：`Assets/Scripts/{Core,Systems,Data}`、`Assets/Prefabs`、`Assets/ScriptableObjects`、`Assets/Art/{Sprites,Tilemaps,UI}`、`Assets/Audio`、`Assets/Scenes/Gameplay`

## 下一步
1. 用户去 WorkBuddy 连接器管理页面 Trust Unity MCP 服务器。
2. 回到 **Phase 1 概念孵化**：spawn design-strategist 带概念简报产出正式概念文档（落 `design/concept/game-concept.md`）。
3. 概念文档确认后 spawn art-director 产出美术圣经。
4. 概念 DNA 仍有效：上帝视角世界模拟·三支柱（经营建设×生存对抗×生态演化）·古代→现代·四层完整（个体生命周期/社会结构/地理生态/时间尺度）。
